ttio.tools.transport_decode_cli
===============================

.. py:module:: ttio.tools.transport_decode_cli

.. autoapi-nested-parse::

   Decode a .tis transport stream into a .tio file ().

   Parallel to Java {@code global.thalion.ttio.tools.TransportDecodeCli}
   and ObjC {@code TtioTransportDecode}.

   Usage:
       python -m ttio.tools.transport_decode_cli <input.tis> <output.tio>



Functions
---------

.. autoapisummary::

   ttio.tools.transport_decode_cli.main


Module Contents
---------------

.. py:function:: main(argv: list[str] | None = None) -> int

   Decode a ``.tis`` transport stream into a ``.tio`` file.

   Parses positional arguments ``input`` and ``output``, then calls
   :func:`ttio.transport.codec.transport_to_file` to materialise the
   stream onto disk and close the resulting dataset.

   :param argv: Argument vector. Defaults to ``sys.argv[1:]`` when ``None``.
   :type argv: list[str], optional

   :returns: *int* -- ``0`` on success. Argparse exits with ``2`` on usage errors.


