ttio.providers.registry
=======================

.. py:module:: ttio.providers.registry

.. autoapi-nested-parse::

   Provider registry + factory — Milestone 39 Part B/E.

   Providers register via ``importlib.metadata`` entry points in the
   ``ttio.providers`` group. In-process registration
   (:func:`register_provider`) is also supported for testing.

   Lookup by provider name (``"hdf5"``, ``"memory"``) or by URL scheme
   (``"file"``, ``"memory"``, ``"s3"``, ``"http"``, ``"https"``).

   Entry points are declared in ``pyproject.toml``:

   .. code-block:: toml

       [project.entry-points."ttio.providers"]
       hdf5 = "ttio.providers.hdf5:Hdf5Provider"
       memory = "ttio.providers.memory:MemoryProvider"

   Cross-language equivalents
   --------------------------
   Objective-C: ``TTIOProviderRegistry`` class ·
   Java: ``com.dtwthalion.ttio.providers.ProviderRegistry`` class ·
   Python: module-level functions
   (``discover_providers``, ``open_provider``, ``register_provider``)
   — idiomatic for Python packaging.

   API status: Stable (Provisional per M39).

   SPDX-License-Identifier: LGPL-3.0-or-later



Functions
---------

.. autoapisummary::

   ttio.providers.registry.register_provider
   ttio.providers.registry.discover_providers
   ttio.providers.registry.open_provider


Module Contents
---------------

.. py:function:: register_provider(name: str, cls: type[ttio.providers.base.StorageProvider]) -> None

   Register ``cls`` under ``name``. Overrides an existing entry.


.. py:function:: discover_providers() -> dict[str, type[ttio.providers.base.StorageProvider]]

   Return the full map of registered providers, loading entry
   points lazily on first call. Subsequent calls reuse the cache.


.. py:function:: open_provider(path_or_url: str, *, provider: str | None = None, mode: str = 'r', **kwargs: Any) -> ttio.providers.base.StorageProvider

   Resolve a provider and open a backing store.

   * ``provider`` — explicit provider name (bypasses URL detection).
   * URL scheme — e.g. ``memory://foo``, ``s3://bucket/key.tio``,
     ``file:///path/to/data.tio``.
   * Otherwise — bare path, routed to the ``hdf5`` provider.


