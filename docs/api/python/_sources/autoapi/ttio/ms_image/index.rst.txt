ttio.ms_image
=============

.. py:module:: ttio.ms_image

.. autoapi-nested-parse::

   ``MSImage`` — mass-spectrometry imaging cube.



Classes
-------

.. autoapisummary::

   ttio.ms_image.MSImage


Module Contents
---------------

.. py:class:: MSImage

   Bases: :py:obj:`ttio.image.Image`


   Mass-spectrometry imaging dataset: a ``width x height`` grid of
   pixels, each pixel a spectral profile of ``spectral_points`` values.

   :param width: Image grid dimensions in pixels.
   :type width: int, default 0
   :param height: Image grid dimensions in pixels.
   :type height: int, default 0
   :param spectral_points: Number of float64 values per pixel.
   :type spectral_points: int, default 0
   :param intensity: Rank-3 intensity cube of shape ``(height, width, spectral_points)``.
   :type intensity: numpy.ndarray, default empty rank-3 array
   :param mz_axis: 1-D float64 array of length ``spectral_points`` giving the m/z
                   calibration for each spectral bin.  Empty when the file was
                   written before format v1.2 (legacy files); use
                   :meth:`to_pixel_spectra` only when this is populated.
   :type mz_axis: numpy.ndarray, default empty 1-D array
   :param pixel_size_x: Spatial pixel size (implementation-defined units).
   :type pixel_size_x: float, default 0.0
   :param pixel_size_y: Spatial pixel size (implementation-defined units).
   :type pixel_size_y: float, default 0.0
   :param scan_pattern: Scan pattern label (e.g. ``"raster"``, ``"random_access"``).
   :type scan_pattern: str, default ""
   :param tile_size: HDF5 chunk tile size for reads; 0 means native cube chunks.
   :type tile_size: int, default 0
   :param title: Dataset-level metadata. In Objective-C these are inherited
                 from ``TTIOSpectralDataset``; in Python they are composed
                 directly onto ``MSImage``.
   :type title: str, default ""
   :param isa_investigation_id: Dataset-level metadata. In Objective-C these are inherited
                                from ``TTIOSpectralDataset``; in Python they are composed
                                directly onto ``MSImage``.
   :type isa_investigation_id: str, default ""
   :param identifications: Dataset-level identifications (composed -- see Notes).
   :type identifications: list, default []
   :param quantifications: Dataset-level quantifications (composed).
   :type quantifications: list, default []
   :param provenance_records: Dataset-level provenance records (composed).
   :type provenance_records: list, default []

   .. rubric:: Notes

   API status: Stable.

   **Composition vs inheritance.** In Objective-C ``TTIOMSImage``
   inherits from ``TTIOSpectralDataset`` so dataset-level fields come
   for free. In Python, ``SpectralDataset`` is a file-handle wrapper
   whose lifecycle does not map cleanly to an MSImage subclass;
   composition is used here (the five dataset-level fields live on
   ``MSImage`` directly). This is a known stylistic difference
   between the language implementations, recorded in
   :doc:`/api-review-v0.6`.

   Cross-language equivalents
   --------------------------
   Objective-C: ``TTIOMSImage`` - Java:
   ``global.thalion.ttio.MSImage``.


   .. py:attribute:: mz_axis
      :type:  numpy.ndarray


   .. py:attribute:: kind
      :type:  ClassVar[ttio.enums.ImageKind]


   .. py:property:: spectral_axis
      :type: numpy.ndarray


      The m/z calibration axis (alias of :attr:`mz_axis`).


   .. py:property:: spectral_axis_kind
      :type: ttio.enums.SpectralAxisKind


      Interpretation of :attr:`spectral_axis` for this modality.


   .. py:method:: __post_init__() -> None


   .. py:method:: write_to(study_group) -> None

      Write this image cube under ``<study_group>/image_cube/``.

      Mirrors :meth:`global.thalion.ttio.MSImage.writeTo` -- intensity
      as a 3-D ``[h, w, sp]`` dataset, optional ``mz_axis`` 1-D
      dataset when populated.



   .. py:method:: read_from(study_group) -> MSImage | None
      :classmethod:


      Read an MSImage cube from a study group; return None if absent.



   .. py:method:: to_pixel_spectra()

      Project this image as a list of continuous-mode pixel records.

      Returns a list of :class:`ttio.importers.imzml.ImzMLPixelSpectrum`
      objects, one per pixel, all sharing :attr:`mz_axis` as their
      ``mz`` array.

      Raises ``RuntimeError`` when ``mz_axis`` is empty (legacy file).



