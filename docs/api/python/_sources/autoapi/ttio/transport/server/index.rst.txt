ttio.transport.server
=====================

.. py:module:: ttio.transport.server

.. autoapi-nested-parse::

   WebSocket transport server ().

   The server hosts a single :class:`SpectralDataset` and accepts one or
   more simultaneous WebSocket clients. Each client sends a JSON query
   message describing optional filters; the server then streams the
   dataset's transport packets back as one binary WebSocket frame per
   packet.

   Wire flow:

   1. Client connects.
   2. Client sends text frame: ``{"type": "query", "filters": {...}}``
      (empty filters = full stream).
   3. Server emits StreamHeader, all DatasetHeaders, then AUs (filtered
      by the query), EndOfDataset per dataset, and EndOfStream.
   4. Connection closes.

   ``StreamHeader`` / ``DatasetHeader`` / ``EndOfDataset`` /
   ``EndOfStream`` are always emitted so the client has a complete
   container skeleton regardless of filter selectivity (§7 of
   ``docs/transport-spec.md``).



Classes
-------

.. autoapisummary::

   ttio.transport.server.TransportServer


Functions
---------

.. autoapisummary::

   ttio.transport.server.serving


Module Contents
---------------

.. py:class:: TransportServer(dataset_source: ttio.spectral_dataset.SpectralDataset | str | pathlib.Path, *, host: str = 'localhost', port: int = 9700)

   WebSocket server streaming a :class:`SpectralDataset` on demand.

   Construct an unstarted server bound to one dataset source.

   :param dataset_source: The dataset to serve. A :class:`SpectralDataset` instance
                          is borrowed (not closed by the server); a string or
                          ``Path`` is opened per client connection and closed when
                          the connection ends.
   :type dataset_source: SpectralDataset, str, or pathlib.Path
   :param host: Interface to bind. Default ``"localhost"``.
   :type host: str, optional
   :param port: TCP port to bind. Pass ``0`` to let the OS pick a free
                port; the chosen port is observable via :attr:`port`
                after :meth:`start` returns. Default ``9700``.
   :type port: int, optional


   .. py:property:: port
      :type: int


      TCP port the server is bound to.

      Reflects the port given at construction until :meth:`start`
      runs, then the kernel-assigned port when ``port=0`` was
      requested. Useful for tests that bind to ``port=0`` and need
      to dial back in.


   .. py:property:: host
      :type: str


      Bind interface for the listening socket.

      Returns the host string given at construction (e.g.
      ``"localhost"`` or ``"0.0.0.0"``). Not mutated by
      :meth:`start`.


   .. py:method:: start() -> None
      :async:


      Start serving and return once the socket is listening.

      Creates the WebSocket server, binds to ``(host, port)``, and
      resolves :attr:`port` to the kernel-assigned value when
      ``port=0`` was passed at construction. The coroutine
      completes as soon as the listener is accepting connections;
      client traffic is then handled in the background until
      :meth:`stop` is called.

      :returns: *None*



   .. py:method:: wait_closed() -> None
      :async:


      Block until the listening socket has been fully closed.

      Use after :meth:`stop` (or in a process-supervision loop
      alongside an external cancel signal) to wait for in-flight
      connections to drain. Asserts the server has been started.



   .. py:method:: stop() -> None
      :async:


      Stop serving and close all active client connections.

      Sends a close frame (code ``1001`` — "server shutdown") to
      every active client, closes the listening socket, and waits
      for the server task to terminate. Idempotent: a second call
      is a no-op once the server has been stopped.



.. py:function:: serving(dataset_source: ttio.spectral_dataset.SpectralDataset | str | pathlib.Path, *, host: str = 'localhost', port: int = 0) -> AsyncIterator[TransportServer]
   :async:


   Async context manager: start a server, yield it, stop on exit.


