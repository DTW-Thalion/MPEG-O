mpeg_o.stream_writer
====================

.. py:module:: mpeg_o.stream_writer

.. autoapi-nested-parse::

   ``StreamWriter`` — incremental append writer for .mpgo files.



Classes
-------

.. autoapisummary::

   mpeg_o.stream_writer.StreamWriter


Module Contents
---------------

.. py:class:: StreamWriter(file_path: str, run_name: str, acquisition_mode: mpeg_o.enums.AcquisitionMode, instrument_config: mpeg_o.instrument_config.InstrumentConfig)

   Incrementally append mass spectra to an ``.mpgo`` file.

   Spectra accumulate in memory until :meth:`flush` is called. On
   each flush the file is rewritten so that the run group reflects
   every spectrum buffered so far — the file remains a valid
   ``.mpgo`` after each flush.

   .. rubric:: Notes

   API status: Stable.

   For v0.6 the writer's flush is whole-file regenerative: simple,
   correct, and bounded for the streaming-demo case (≤ a few
   thousand spectra). A future milestone may switch to extendable
   HDF5 datasets.

   Cross-language equivalents
   --------------------------
   Objective-C: ``MPGOStreamWriter`` · Java:
   ``com.dtwthalion.mpgo.StreamWriter``.


   .. py:method:: append_spectrum(spectrum: mpeg_o.mass_spectrum.MassSpectrum) -> None

      Buffer a spectrum. Call :meth:`flush` to persist.



   .. py:property:: spectrum_count
      :type: int


      Number of buffered spectra.


   .. py:method:: flush() -> None

      Rewrite the target file with all buffered spectra so far.

      Converts each buffered :class:`~mpeg_o.mass_spectrum.MassSpectrum`
      into an :class:`~mpeg_o.importers.import_result.ImportedSpectrum`,
      packs them into a :class:`~mpeg_o.spectral_dataset.WrittenRun`, and
      delegates to :meth:`~mpeg_o.spectral_dataset.SpectralDataset.write_minimal`
      so the target file is a valid ``.mpgo`` after each call.



   .. py:method:: flush_and_close() -> None

      Flush one final time and release resources.



   .. py:method:: close() -> None

      Release resources without flushing.



