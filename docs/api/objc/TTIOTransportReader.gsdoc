<?xml version="1.0"?>
<!DOCTYPE gsdoc PUBLIC "-//GNUstep//DTD gsdoc 1.0.4//EN" "http://www.gnustep.org/gsdoc-1_0_4.dtd">
<gsdoc base="TTIOTransportReader" stylesheeturl="default-styles.css">
  <head>
    <title>TTIOTransportReader documentation</title>
    <author name="Generated by "></author>
    <date>Generated at 2026-06-05</date>
  </head>
  <body>
    <front><contents /></front>
    <chapter>
      <heading>
        Software documentation for the
        TTIOTransportPacketRecord class
      </heading>
      <class name="TTIOTransportPacketRecord" super="NSObject">
        <declared>TTIOTransportReader.h</declared>
        <desc>
          <p>
            <em>Inherits From:</em> NSObject
          </p>
          <p>
            <em>Conforms To:</em> NSObject (NSObject)
          </p>
          <p>
            <em>Declared In:</em> Transport/TTIOTransportReader.h
          </p>
          <p>
            One parsed packet as a header + payload pair.
          </p>
        </desc>
        <method type="TTIOTransportPacketHeader*">
          <sel>header</sel>
          <desc>
            Getter for property 'header' with attributes
            (strong, nonatomic, readonly).
          </desc>
        </method>
        <method type="instancetype">
          <sel>initWithHeader:</sel>
          <arg type="TTIOTransportPacketHeader*">h</arg>
          <sel>payload:</sel>
          <arg type="NSData*">p</arg>
          <desc>
            Construct a packet record from a header + payload
            pair. @param <var>h</var> Decoded packet header.
            @param <var>p</var> Payload bytes; size must equal
            ``h.payloadLength``. @return
            Initialised record instance.
          </desc>
        </method>
        <method type="NSData*">
          <sel>payload</sel>
          <desc>
            Getter for property 'payload' with attributes
            (strong, nonatomic, readonly).
          </desc>
        </method>
      </class>
    </chapter>
    <chapter>
      <heading>
        Software documentation for the TTIOTransportReader
        class
      </heading>
      <class name="TTIOTransportReader" super="NSObject">
        <declared>TTIOTransportReader.h</declared>
        <desc>
          <p>
            <em>Inherits From:</em> NSObject
          </p>
          <p>
            <em>Conforms To:</em> NSObject (NSObject)
          </p>
          <p>
            <em>Declared In:</em> Transport/TTIOTransportReader.h
          </p>
          <p>
            Parses a transport byte stream into packet (header,
            payload) pairs or materialises the stream into a
            new <code>.tio</code> file.
          </p>
          <p>
            <strong>Cross-language equivalents:</strong>
            <br /> Python:
            <code>ttio.transport.codec.TransportReader</code>
            <br /> Java:
            <code>
              global.thalion.ttio.transport.TransportReader
            </code>
          </p>
        </desc>
        <method type="instancetype">
          <sel>initWithData:</sel>
          <arg type="NSData*">data</arg>
          <desc>
            Initialise a reader sourcing from an in-memory
            byte buffer. The buffer is borrowed; the reader never
            mutates it. Useful for round-tripping bytes
            produced by ``-[TTIOTransportWriter
            initWithMutableData:]``. @param
            <var>data</var> The full transport-stream byte
            buffer. @return Initialised reader instance.
          </desc>
        </method>
        <method type="instancetype">
          <sel>initWithInputPath:</sel>
          <arg type="NSString*">path</arg>
          <desc>
            Initialise a reader sourcing from a ``.tis``
            file on disk. The file is opened lazily on the first
            read and closed when the reader deallocates. @param
            <var>path</var> Filesystem <var>path</var> of the
            ``.tis`` file. @return Initialised reader
            instance.
          </desc>
        </method>
        <method type="NSArray&lt;TTIOTransportPacketRecord*&gt;*">
          <sel>readAllPacketsWithError:</sel>
          <arg type="NSError**">error</arg>
          <desc>
            Read every packet in the stream into an array of
            records. Returns all packets through (and
            including) ``END_OF_STREAM``. Unknown packet
            types are kept in the returned list for
            forward-compat per transport-spec §6.
            CRC-32C trailers are verified when ``HAS_CHECKSUM``
            is set on the header. @param <var>error</var> On
            failure, populated with an ``NSError`` in
            ``TTIOTransportErrorDomain``.
            May be ``NULL``. @return Ordered packet array on
            success, or ``nil`` on CRC-32C mismatch /
            structural violation / truncated stream (and
            ``*error`` is set if non-NULL).
          </desc>
        </method>
        <method type="BOOL">
          <sel>writeTtioToPath:</sel>
          <arg type="NSString*">outputPath</arg>
          <sel>error:</sel>
          <arg type="NSError**">error</arg>
          <desc>
            Materialise the transport stream into a
            ``.tio`` file. Drains every packet, accumulates
            per-dataset state, and writes the result via
            the HDF5 provider. Scope: HDF5 provider; float64 +
            ``NONE``/``ZLIB`` compression; the
            spectral, genomic, image, reference,
            identifications, quantifications,
            subjects, samples and provenance payloads
            documented in ``docs/transport-spec.md`` are all
            supported. @param <var>outputPath</var>
            Destination ``.tio`` path. Created if absent;
            truncated if present. @param <var>error</var> On
            failure, populated with an ``NSError``. May be
            ``NULL``. @return ``YES`` on success, ``NO`` on
            failure (and ``*error`` is set if non-NULL).
          </desc>
        </method>
      </class>
    </chapter>
  </body>
</gsdoc>
