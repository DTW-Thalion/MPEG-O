# M41.9 — Docs Assembly

**Goal:** Final slice of Milestone 41 — write the two deliverable
docs required by the M41 design spec.

## Deliverables

1. docs/api-review-v0.6.md — three-column parity map across
   all eight code-touching slices (41.1-41.8), with a Known
   Stylistic Differences section and an appendix describing the
   audit methodology.

2. docs/migration-guide.md — Python-focused mzML → TTI-O and
   nmrML → TTI-O quickstart with runnable snippets + semantic
   mapping tables + round-trip verification patterns.

## M41 completion

- Every slice 41.1-41.8 shipped as a separate commit.
- Test counts end-of-M41: ObjC 867, Python 184, Java 126, cross-
  compat 8/8.
- Three-way cross-compat behavior unchanged (no wire-format
  changes across the milestone).
- HANDOFF.md updated to record M41 shipped, v0.6.0 tag pending
  user approval.

## Deferred items recorded in the docs

- Encryptable.encrypt_with_key / decrypt_with_key on
  AcquisitionRun + SpectralDataset pending persistence-context
  wiring in Python/Java.
- StreamWriter.flush pending SpectralDataset.write integration.
- Thermo reader is a stub in ObjC (live in Python/Java).
- Java CVTermMapper.precisionFor returns null for unknown (vs
  FLOAT64 in ObjC/Python) — real divergence documented.
- Python ImportResult DTO is Python-idiomatic with no ObjC/Java
  equivalent.
- Maven Central groupId migration (com.dtwthalion → global.thalion)
  pending M40.

## Acceptance

- Both deliverable docs committed.
- HANDOFF.md updated.
- Plan + commit recorded.
- ObjC 867 / Python 184 / Java 126 unchanged (docs-only slice).
- Cross-compat 8/8.
