# W5.7 progress — closes W5

Status snapshot for the **Encoding + Export Panels + smoke**
sub-phase per [`docs/workbench-client/W5-plan.md`](W5-plan.md).
This is the final W5 sub-phase.

## Status (2026-05-19): COMPLETE -- in review. **W5 DONE.**

Adds the last two spec section 8.1 GUI components, bumps
tio-browser to 1.5.0, and lands the GUI-assembly end-to-end
smoke.

## Deliverables

### tio-browser GUI (`workbench/`)

| File | Purpose |
|---|---|
| `EncodingPanel.java` | Modal encode + upload coordinator. Source file picker -> `FormatSniffer` format detect -> Phase-8 `ImportTask` encode to a temp `.tio` -> W5.3 `TransferManager` upload under a derived container URI. Static helpers `deriveContainerUri`, `deriveTempTio`, `isValidProject`. |
| `ExportPanel.java` | Modal client-side export. Opens a local `.tio` (typically W5.3-downloaded) -> Phase-9 `ExportTask` to a target format. Static helpers `extensionFor`, `deriveExportTarget`, `isValidTioPath`. |

### MainWindow

Two new `Workbench` menu items: `Encode + upload...` and
`Export container...`.

### Version

`tio-browser/pom.xml` 1.4.1 -> 1.5.0. The java/python ttio
library stays at 1.3.0 (W5.0 SDK bump); tio-browser versions
independently per the carry-forward rule.

## Tests

- `EncodingPanelTest` (9): `deriveContainerUri` (name / path-strip
  / lowercase-hyphenate / no-project / empty-base / Windows
  backslash); `deriveTempTio`; `isValidProject`.
- `ExportPanelTest` (11): `extensionFor` (known + fallback);
  `deriveExportTarget` (swap / dir-preserve / no-dir / null);
  `isValidTioPath`.
- `WorkbenchMenuSmokeTest` (2 TestFX): the Workbench menu exposes
  every W5.1-W5.7 action; every action has an onAction handler.

## W5 acceptance (W5-plan.md)

- [x] Every component in spec section 8.1 has a working
      tio-browser panel. **9/9 done** (see component table below).
- [x] GUI-assembly verified by `WorkbenchMenuSmokeTest`.
- [ ] One operator completes UC-01 -> UC-04 -> UC-07 -> UC-09 ->
      UC-04 entirely in the GUI. **Deferred:** needs a live
      workbench-server in CI (the shared cross-W follow-up). The
      panels are all wired; the end-to-end *live* run is the
      remaining gate.

## Spec section 8.1 component coverage (W5 COMPLETE)

| Component | Sub-phase | PR |
|---|---|---|
| Connection Manager | W5.1 | #101 |
| Container Browser | W5.2 | #102 |
| Upload/Download Manager | W5.3 | #103 |
| Selective Access Panel | W5.3 | #103 |
| Cohort Query Builder | W5.4 | #104 |
| Pipeline Launcher | W5.5 | #106 |
| Job Monitor | W5.5 | #106 |
| Interactive Session Launcher | W5.6 | #107 |
| Encoding Panel | W5.7 | this PR |
| Export Panel | W5.7 | this PR |

## Cross-W deferred follow-ups (consolidated)

These were deferred consistently across W5 sub-phases; they are
genuine v1.1 / server-dependent items, not gaps in the v1.0
client surface:

1. **Live-daemon round-trip smoke** -- needs the
   `tti-workbench-server` Docker image vendored in CI. Blocks
   the final W5 acceptance "operator completes the full flow"
   item. Same shape as the W1/W3/W4 SDK live-daemon follow-ups.
2. **Schema-driven pipeline inputs form** (W5.5) -- v1.0 uses
   raw JSON textareas.
3. **Save-as-cohort** (W5.4) -- needs server v1.1
   `POST /v1/cohorts`.
4. **Embedded Jupyter WebView** (W5.6) -- spec 7.4 step 3
   option a; v1.0 ships system-client attach.
5. **Server-side export pipeline** (W5.7) -- v1.0 ships
   client-side export.
6. **Tree-style cohort editor with nested composites** (W5.4).
7. **Unified DataSourceTree** merging local + remote (W5.2).

## What's next

W5 closes the GUI track. The remaining workplan milestone is
**W6** (SDK polish + remaining formats + PQC + federation
client), which reaches into the spec's Phase 5 production
hardening. W6 is independent of W5 and can start whenever the
user chooses.

The live-daemon CI follow-up (item 1 above) is the highest-value
remaining work to close the W1-W5 acceptance gates -- it would
turn every "implementation present; live verification deferred"
checkbox green across all five client milestones at once.
