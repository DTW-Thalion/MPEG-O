"""Cython acceleration for :mod:`ttio.codecs.delta_rans`.

This package only carries the compiled extension; the public API lives
in :mod:`ttio.codecs.delta_rans`. Importing this package directly is not
required — the wrapper detects and uses the extension automatically when
present.
"""
